<!-- handoff:task:b219a452-501e-4414-a736-f15a576d57fe -->
# Очистить комментарии о минифицированных чанках

**Создано:** 2026-07-24
**Ветка:** `main`
**Тип:** рефакторинг комментариев без изменения поведения

## Settings

- [ ] **Testing:** no — задача меняет только комментарии; новые и существующие тесты не изменять и не запускать.
- [ ] **Logging:** verbose (policy) — runtime-логирование не затрагивать и не добавлять; для всех задач ниже логирование не применимо.
- [ ] **Docs:** no — не изменять пользовательскую документацию, `.ai-factory/MIGRATION_STATUS.md`, `DESCRIPTION.md`, `ARCHITECTURE.md` и `ROADMAP.md`.

## Roadmap Linkage

- [ ] **Milestone:** `none`
- [ ] **Rationale:** автономный Handoff-режим использует настройку без привязки к roadmap; задача соответствует пункту этапа 9, но не меняет сам roadmap.

## Scope and Rules

- [ ] Текущая база: 111 вхождений в 108 исходных файлах — 92 в `packages/editor/src`, 13 в `packages/schema/src` и 3 в `apps/playground/src`.
- [ ] Работать только с комментариями в исходниках; не менять исполняемый код, импорты, экспорты, типы, стили, шаблоны и форматирование, не относящееся к комментарию.
- [ ] Удалять комментарий целиком, если он сообщает только происхождение вида «порт из чанка …, модуль …» или другой технический идентификатор минифицированной сборки.
- [ ] Если комментарий также объясняет назначение, алгоритм, инвариант или отличие от внешней библиотеки, сохранить это знание кратким русскоязычным пояснением и убрать идентификаторы чанков/модулей и формулировки о минифицированной сборке.
- [ ] Не трогать исторические записи и артефакты вне scope: `.ai-factory/**`, `packages/*/dist/**`, `dist/**`, `coverage/**` и `node_modules/**`.
- [ ] Перед правкой сохранять существующие незавершённые изменения в рабочем дереве: применять только точечные изменения строк комментариев.

## Tasks

### Phase 1 — Компоненты редактора

- [x] **1. Очистить provenance-комментарии в Vue-компонентах редактора.**
  - [ ] **Files:** `packages/editor/src/components/**/*.vue`, включая `notion/**`, `primitives/**`, `table/**` и `ui/**` (61 файл по текущему поиску).
  - [ ] Удалить заголовки и однострочные комментарии, которые содержат только ссылку на чанк/модуль исходной минифицированной сборки.
  - [ ] В смешанных комментариях оставить по-русски только полезное описание роли компонента, его UI-состояния, взаимодействия с редактором или пользовательского эффекта.
  - [ ] Не менять код в `<script setup>`, шаблоны и CSS; не добавлять новые runtime-логи и не менять существующие.
  - [ ] **Dependency:** нет.

### Phase 2 — Логика пакета editor

- [x] **2. Нормализовать комментарии composables, node views и utilities редактора.**
  - [ ] **Files:** `packages/editor/src/composables/**/*.ts`, `packages/editor/src/nodes/**/*.vue`, `packages/editor/src/utils/**/*.ts` и `packages/editor/src/extensions/**/*.ts` (31 файл по текущему поиску).
  - [ ] Для `use*` composables и утилит сохранить русские пояснения о назначении, ограничениях и важных отличиях реализации, но убрать provenance минифицированных чанков и номера модулей.
  - [ ] В `utils/suggestion/**`, позиционировании таблиц и других сложных местах не сокращать существующие смысловые пояснения до общих фраз; удалять только сведения о происхождении сборки.
  - [ ] Не добавлять и не менять runtime-логи; изменения ограничить комментариями.
  - [ ] **Dependency:** задача 1 использует те же правила редакторских комментариев и должна быть завершена первой.

### Phase 3 — Пакет schema

- [x] **3. Очистить комментарии о чанках в расширениях, нодах и утилитах schema.**
  - [ ] **Files:** `packages/schema/src/extensions/**/*.ts`, `packages/schema/src/nodes/**/*.ts`, `packages/schema/src/utils/**/*.ts` (13 файлов по текущему поиску).
  - [ ] Сохранить русские описания публичного назначения расширений, ограничений ProseMirror/Tiptap и контрактов утилит; удалить номера модулей, идентификаторы чанков и утверждения о «дословном порте» как неактуальные для сопровождения.
  - [ ] В точечных JSDoc, где идентификатор был единственным содержанием, удалить комментарий вместо подмены его формальным текстом.
  - [ ] Не изменять exports, runtime-логи или реализацию schema.
  - [ ] **Dependency:** задача 2 задаёт единый стиль пояснений для смежной редакторской логики.

### Phase 4 — Playground и проверка полноты

- [x] **4. Привести к тем же правилам комментарии playground.**
  - [ ] **Files:** `apps/playground/src/components/CtaPopup.vue`, `apps/playground/src/components/NotionEditorHeader.vue`, `apps/playground/src/content/default-content.ts`.
  - [ ] Убрать ссылки на минифицированные чанки; оставить только русские комментарии, которые объясняют демонстрационный контент или поведение экрана.
  - [ ] Не менять тексты интерфейса, demo-данные, разметку и runtime-логи.
  - [ ] **Dependency:** задачи 1–3, чтобы правила формулировок были одинаковыми во всех пакетах.

- [x] **5. Проверить отсутствие ссылок на provenance минифицированной сборки и чистоту diff (повторно 2026-07-24).**
  - [ ] **Files:** все файлы из задач 1–4; не расширять scope на документацию и generated output.
  - [ ] Повторить поиск по `packages/*/src/**` и `apps/*/src/**` на сочетания «порт/эквивалент/дословный порт» с `чанк` или `минифицирован`, а также на идентификаторы бывших чанков и номера модулей в комментариях; все оставшиеся результаты вручную классифицировать и устранить, если это provenance.
  - [ ] Проверить, что у оставшихся комментариев есть понятная русскоязычная смысловая нагрузка и ни один не ссылается на минифицированную сборку.
  - [ ] Выполнить `git diff --check -- packages/editor/src packages/schema/src apps/playground/src`; не запускать тесты и не добавлять тестовые изменения.
  - [ ] Runtime-логи не применимы; подтвердить, что в diff отсутствуют изменения логирования и исполняемого кода.
  - [ ] **Dependency:** задачи 1–4.

## Commit Plan

1. После задач 1–4: `chore(comments): remove minified chunk references` — только целевые комментарии исходников.
2. После задачи 5: не создавать отдельный коммит, если проверка не потребовала дополнительных точечных правок; включить их в первый коммит.

## Completion Criteria

- [x] В исходниках `packages/editor/src`, `packages/schema/src` и `apps/playground/src` нет комментариев с идентификаторами чанков/модулей минифицированной сборки или ссылок на их provenance.
- [x] Полезные комментарии объясняют текущую логику на русском языке, а бессодержательные удалены.
- [x] Diff состоит только из комментариев в оговорённых исходниках и проходит `git diff --check`.
- [x] Тесты, пользовательская документация, roadmap и миграционные артефакты не изменены.
