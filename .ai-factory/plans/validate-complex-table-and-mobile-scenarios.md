<!-- handoff:task:b44258ab-8d0f-48d7-b7ce-0f1f1a198583 -->
# Implementation Plan: Validate Complex Table and Mobile Scenarios

Branch: `main`
Created: 2026-07-28

## Settings

- [ ] Testing: focused regression coverage was added only after confirming the rowspan/colspan deletion defect; this rework does not add automated coverage for physical-device behavior.
- [ ] Logging: verbose — use the existing structured loggers and development diagnostics only when needed to isolate a reproduced issue.
- [ ] Docs: no — do not update documentation or create a standalone QA report.

## Roadmap Linkage

Milestone: "Этап 10. Технический долг"
Rationale: This plan executes the roadmap item to validate `rowspan`/`colspan` table behavior and mobile gestures, then correct confirmed discrepancies.

## Validation Contract

- [x] Use `npm run dev` to exercise the current playground at `apps/playground/src/App.vue`; do not commit a fixture or playground-only controls unless a confirmed defect requires them.
- [ ] Validate on a desktop baseline and on touch-capable mobile browsers: iOS Safari at a narrow viewport (for example, 375 px) and Android Chrome at a narrow viewport (for example, 360 px). Use physical devices when available; device emulation alone is not sufficient evidence for touch behavior.
- [x] Build a named-content table that includes a 2×2 merged cell (`rowspan="2"`, `colspan="2"`), an independent horizontal merge, and an independent vertical merge. Populate every logical cell so unintended data loss and placement changes are observable.
- [x] Treat an issue as confirmed only when it survives a clean reload and has a minimal action sequence. Capture the viewport, input method, selected cell/range, action, expected result, and actual result in the corresponding task completion; do not create a separate report artifact.
- [x] Preserve public document contracts: exported JSON and rendered HTML must retain valid `rowspan`/`colspan` semantics, and editor CSS must remain scoped below `.tinyfy-editor`.

## Tasks

### Phase 1: Establish Complex-Table Baseline

- [x] **Task 1: Create and baseline a complex table fixture in the running playground.**
  - [x] Exercise `apps/playground/src/App.vue` and the editor composition path in `packages/editor/src/components/notion/notion-editor/EditorContentArea.vue`; no source file changes are expected for this task.
  - [x] Start from the live named 5×4 playground table, create the required 2×2, independent horizontal, and independent vertical spans, and retain representative header, alignment, background, and column-width values.
  - [x] Confirm initial visual grid geometry, keyboard caret movement, cell selection, JSON/HTML serialization, and static rendering through the public `packages/renderer/src/index.ts` module.
  - [x] **Logging:** captured only bounded viewport, selection-count, table-dimension, and browser-error metadata; no document JSON/HTML or host data was logged in the plan.
  - [x] **Dependencies:** none.
  - [x] **QA evidence — passed on 2026-07-28:** `npm run dev` served the playground at `http://127.0.0.1:5173`. Repository-local Chromium launched with the pre-existing cache-only Debian runtime bundle. At desktop width 1440 px, the live 5×4 named table retained four distinct column widths (154/185/154/156 px) and headers. Shift-click selected exactly 4 cells for the 2×2 merge and exactly 2 cells for each independent horizontal and vertical merge. The resulting fixture preserved `Alice Johnson/Senior Developer/Bob Smith/Product Manager` at `colspan="2" rowspan="2"`, `Carol White/UX Designer` at `colspan="2" rowspan="1"`, and `Design/Analytics` at `colspan="1" rowspan="2"`. The `Name` header retained a blue background and centered alignment in JSON; arrow-key navigation moved the text selection from position 1433 to 1445. The public renderer retained all three span patterns. The only live-browser error was an unrelated `404` resource request; no table/action error was emitted.

### Phase 2: Validate Interactive Table Semantics and Rendering

- [x] **Task 2: Run the desktop complex-table checklist and isolate any table-specific failure.**
  - [x] Exercise table extension and command surfaces in `packages/schema/src/extensions/table-kit.ts`, `packages/editor/src/extensions/table-kit.ts`, `packages/editor/src/utils/table-actions/`, `packages/editor/src/components/table/`, `packages/editor/src/composables/useTableFitToWidth.ts`, and `packages/editor/src/styles/table.css`; changed `packages/editor/src/utils/table-actions/add-delete.ts` and `packages/editor/src/utils/table-actions/shared.ts`, then added focused regression coverage in `test/editor/utils/table-actions.test.ts` after reproducing a span-specific deletion defect.
  - [x] Verify that merge/split, row/column insertion and deletion adjacent to spans, header toggles, sorting/movement where available, cell alignment, clear-content operations, and undo/redo preserve a valid rectangular table map and the expected cell contents.
  - [x] Verify rendering after horizontal scrolling, column resizing, table fitting, and selection changes: the prior desktop geometry pass and this action pass found the wrapper, overlays, handles, and merged-cell boundaries aligned without clipping, gaps, overlap, duplicated borders, or shifted hit areas.
  - [x] Reload and rehydrate the captured content, then confirm exported JSON and renderer output still contain correct `rowspan` and `colspan` attributes without document corruption.
  - [x] **Logging:** the reproduced issue used bounded table dimensions, action names, selection shape, and browser-error metadata only; no document payloads or host data were recorded. No new diagnostic path was needed after the repair.
  - [x] **Dependencies:** Task 1.
  - [x] **QA evidence — passed after repair on 2026-07-28:** On the live 1440 px Chromium playground, merge/split with undo/redo, span-adjacent row and column insertion/deletion, row movement, header-row/header-column toggles, row/column sorting, and clear-content all retained a rectangular table map and named cell contents with no browser console errors. Rework validation added deterministic coverage for a target row fully covered by `rowspan="2" colspan="3"` and a target column fully covered by `rowspan="3" colspan="2"`; each now deletes exactly one resolved line and reduces the affected span to one. `deleteRowColumn` resolves the requested row or column and uses one exact table-map transaction for every deletion, so no `TextSelection` or `CellSelection` rectangle can expand the operation beyond the resolved line.
  - [x] **TDD evidence — RED then GREEN on 2026-07-29:** Before reapplying the production repair, a controlled reproduction temporarily restored the prior `deleteRowColumn` selection-based `deleteRow`/`deleteColumn` path. **RED:** `npx vitest run test/editor/utils/table-actions.test.ts -t "deletes only the resolved line when every target cell spans that axis"` exited 1: the all-rowspan case expected table dimensions `[2, 3]` but received `[1, 3]` at `test/editor/utils/table-actions.test.ts:972`, proving the old path deleted the merged cell’s source row rather than only the resolved line. **GREEN:** restoring `deleteResolvedTableLine` made the identical command exit 0 with 1 test passed and 20 skipped; `npx vitest run test/editor/utils/table-actions.test.ts` then passed all 21 tests, and `npx eslint packages/editor/src/utils/table-actions/add-delete.ts packages/editor/src/utils/table-actions/shared.ts` exited 0.

### Phase 3: Validate Mobile Touch and Gesture Behavior

- [ ] **Task 3: Run the mobile table and toolbar checklist on real touch-capable browsers.**
  - [ ] Exercise the responsive and lazy-mount path in `packages/editor/src/components/notion/notion-editor/EditorContentArea.vue`, `packages/editor/src/components/ui/mobile-toolbar/MobileToolbar.vue`, `packages/editor/src/components/table/table-overlays/TableOverlays.vue`, and `packages/editor/src/styles/table.css`; no source changes are expected for this task.
  - [ ] At ≤480 px, verify the mobile toolbar mounts when editable, stays visible above the keyboard/caret as a user enters a merged cell, returns to its main view after viewport changes, and does not cover table content or intercept table interactions. At >480 px, confirm it is absent.
  - [ ] In the complex fixture, verify tap-to-focus/edit in ordinary and merged cells; horizontal swiping inside the table scrolls the table without trapping vertical page scroll; vertical scrolling outside the table remains natural; and selection/long-press behavior neither produces stale overlays nor corrupts the selection.
  - [ ] Check touch access to every table affordance intentionally exposed on mobile. Desktop-only hover/drag controls must either have an accessible touch route or remain hidden and non-intercepting; they must not create invisible hit targets, text-selection locks, or accidental structural changes.
  - [ ] Repeat the critical interactions after orientation/viewport changes and after closing the virtual keyboard, then reload to confirm the document remains intact.
  - [ ] **Logging:** collect DEBUG-level viewport, breakpoint, toolbar-view, gesture-direction, and selection-shape metadata through existing diagnostics when available. Capture console or structured ERROR output for failures only, without document contents or host secrets.
  - [ ] **Dependencies:** Tasks 1 and 2.
  - [ ] **QA evidence — prior availability check recorded on 2026-07-28T23:39:17Z:** This earlier environment check found no `adb`, `idevice_id`, `xcrun`, `ios-deploy`, `appium`, or local WebDriver executable; `/dev/bus/usb`, `/dev/serial/by-id`, `/var/run/usbmuxd`, and `/run/usbmuxd` are absent; and neither environment variables nor repository configuration reference BrowserStack, Sauce Labs, LambdaTest, Kobiton, Perfecto, AWS Device Farm, or another real-device provider. `playwright.config.ts` defines only a local `chromium` project, and installed Playwright `1.62.0` is not a connected iOS Safari or Android Chrome device. Therefore real-device iOS Safari and Android Chrome validation cannot be completed or claimed here. The prior 375 px iPhone-profile and 360 px Pixel-profile Chromium emulation remains supplemental only; it does not validate the virtual keyboard, trusted horizontal/vertical gestures, long-press selection, or native iOS/Android browser behavior. Physical-device evidence is still required before Task 3 can be checked; Task 4's dependent mobile repair/revalidation remains pending.

  - [ ] **Fresh rework availability evidence — unresolved as of 2026-07-29:** The July 29 rerun found no `adb`, `idevice_id`, `xcrun`, `ios-deploy`, `appium`, `chromedriver`, `geckodriver`, `safaridriver`, or Selenium runner; no USB or `usbmuxd` device endpoint; no relevant environment variable; and no repository configuration for BrowserStack, Sauce Labs, LambdaTest, Kobiton, Perfecto, AWS Device Farm, or another device provider. `playwright.config.ts` still configures only local Chromium. The focused command `npx vitest run test/editor/utils/table-actions.test.ts` and the implicated-source ESLint command are supplemental only, not evidence of real iOS Safari or Android Chrome touch behavior. Task 3 cannot be completed or claimed until an iPhone running Safari and an Android device running Chrome are reachable through a physical-device bridge or authenticated device cloud; Task 4 remains pending that validation.
  - [ ] **Rework preflight — 2026-07-29T01:34:25Z (blocking finding `eee68f94fc7b`):** This execution checked for `adb`, `idevice_id`, `xcrun`, `ios-deploy`, `appium`, `chromedriver`, `geckodriver`, `safaridriver`, and a Selenium runner; none is installed. `/dev/bus/usb`, `/dev/serial/by-id`, `/var/run/usbmuxd`, and `/run/usbmuxd` are absent, and no relevant device-cloud environment variable or repository configuration exists. `playwright.config.ts` exposes only local Desktop Chromium, not a real iOS Safari or Android Chrome target. `npx vitest run test/editor/utils/table-actions.test.ts` passed 21 tests and `npx eslint packages/editor/src/utils/table-actions/add-delete.ts packages/editor/src/utils/table-actions/shared.ts` exited successfully; these checks validate the desktop repair only. They cannot replace real-device tap, trusted horizontal/vertical scroll, long-press selection, orientation, or virtual-keyboard QA. Task 3 and Task 4’s dependent mobile repair/revalidation remain unchecked. To unblock them, connect an iPhone/Safari and Android/Chrome through a physical-device bridge or authenticated real-device cloud, then record every checklist result and repair/revalidate any reproduced discrepancy.

### Phase 4: Repair Confirmed Discrepancies and Revalidate

- [ ] **Task 4: Implement the smallest root-cause fixes and rerun the affected checklists.**
  - [x] For document-model or span-operation failures, change only the implicated files under `packages/schema/src/extensions/table-kit.ts`, `packages/schema/src/utils/table-utils/`, `packages/editor/src/extensions/table-kit.ts`, or `packages/editor/src/utils/table-actions/`.
  - [ ] For geometry, scrolling, overlay, or hit-target failures, change only the implicated files under `packages/editor/src/components/table/`, `packages/editor/src/composables/useTableFitToWidth.ts`, and `packages/editor/src/styles/table.css`.
  - [ ] For responsive toolbar or touch-event failures, change only the implicated files under `packages/editor/src/components/notion/notion-editor/EditorContentArea.vue` and `packages/editor/src/components/ui/mobile-toolbar/`.
  - [x] Preserve valid existing behavior, the public JSON/HTML contract, and `.tinyfy-editor` style isolation. The repair is limited to resolving the intended row/column before deletion.
  - [x] Rerun the exact reproduced desktop action sequence after the fix, including a clean page reload and rectangular-map/content verification. No temporary diagnostic instrumentation remains; mobile regression checks remain blocked by Task 3 physical-device requirements.
  - [x] **QA evidence — desktop repair originally passed on 2026-07-28 and was revalidated on 2026-07-29; mobile follow-up pending:** The merged-only row/column deletion regression was repaired in `packages/editor/src/utils/table-actions/add-delete.ts` and `shared.ts`. On this July 29 rework, `npx vitest run test/editor/utils/table-actions.test.ts` passed all 21 tests and `npx eslint packages/editor/src/utils/table-actions/add-delete.ts packages/editor/src/utils/table-actions/shared.ts` passed with no findings. No mobile production change is justified until real iOS Safari and Android Chrome validation is available.
  - [ ] **Rework disposition — 2026-07-29:** No mobile discrepancy can be confirmed or repaired without the real-device execution required by Task 3. No mobile production change was made, avoiding an unverified behavior change; its dependent mobile repair and revalidation remain pending. The focused table regression and implicated source lint are recorded in Task 3's rework availability evidence.
  - [ ] **Rework status — 2026-07-29T01:34:25Z (blocking finding `eee68f94fc7b`):** No real-device iOS Safari or Android Chrome session was reachable, so no mobile discrepancy could be reproduced and no mobile change is justified. After real-device Task 3 completes, rerun all affected touch/gesture cases after the smallest necessary repair, including orientation changes, virtual-keyboard dismissal, and a clean reload, before checking this task.
  - [x] **Logging:** the structured DEBUG event `delete-resolved-table-line` records only the resolved orientation and index before the exact table-map transaction; existing ERROR logging remains in the exception path. No document contents or host data are logged.
  - [ ] **Dependencies:** Tasks 2 and 3.
