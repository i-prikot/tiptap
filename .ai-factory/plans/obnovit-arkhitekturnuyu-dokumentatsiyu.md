<!-- handoff:task:ba2e63cf-143d-4830-8795-62d4b32ac450 -->
# Implementation Plan: Update Architecture Documentation

Branch: `main`
Created: 2026-07-27

## Settings
- [ ] Testing: no
- [ ] Logging: verbose
- [ ] Docs: no — no additional documentation checkpoint is required; this plan's deliverables are the requested documentation updates.

## Roadmap Linkage
Milestone: "none"
Rationale: Skipped in autonomous Handoff mode; this task keeps the existing roadmap unchanged.

## Scope and Sources of Truth
- [ ] Update only `.ai-factory/DESCRIPTION.md` and `.ai-factory/ARCHITECTURE.md`; do not change package manifests, source code, generated `dist/` files, or the roadmap.
- [ ] Treat the current workspace manifests and public entry points as authoritative: root `package.json`, `packages/{schema,editor,renderer}/package.json`, `apps/playground/package.json`, `packages/schema/src/{index,renderer}.ts`, `packages/editor/src/index.ts`, `packages/editor/src/components/notion/notion-editor/public-api.ts`, and `packages/renderer/src/index.ts`.
- [ ] Preserve the product-level explanation in `DESCRIPTION.md`; move implementation topology, dependency direction, ownership boundaries, and integration flows into `ARCHITECTURE.md`.

## Tasks

### Phase 1: Project Description
- [x] **Task 1: Reframe the project description as a packaged npm-workspaces monorepo.** Update `.ai-factory/DESCRIPTION.md` so the opening purpose, technology overview, repository structure, and consumer context describe the current product rather than a standalone Vue application. Document the four workspace units and their externally meaningful roles: `@i-prikot/editor-schema` owns persisted-document schema versions, migrations, shared extension kits, and renderer-safe exports; `@i-prikot/editor` supplies the reusable Vue editor UI, public `NotionEditor` facade, localized UI, and scoped editor CSS; `@i-prikot/editor-renderer` converts persisted JSON to static HTML on a server-safe path; `@i-prikot/playground` is a private integration/demo application and is not a published library. State that the Tinyfy cabinet is an external host, not an application embedded in this repository. **Expected behavior:** a reader can determine what is publishable, what is development-only, and which package to select for interactive editing versus static rendering. **Files:** `.ai-factory/DESCRIPTION.md`. **Dependencies:** none. **Logging:** no runtime behavior changes and no new logs; retain only existing documentation statements about opt-in redacted development diagnostics, and never document logging of document content, tokens, or localization payloads.

### Phase 2: Monorepo Architecture and Tinyfy Contracts
- [x] **Task 2: Replace the app-centric architecture with package boundaries, dependency rules, and delivery topology.** Restructure `.ai-factory/ARCHITECTURE.md` around the npm workspace root, `packages/`, and `apps/`; retain useful editor-internal detail only under the `@i-prikot/editor` ownership boundary. Add an accurate package dependency diagram and rules: `editor-schema` is the shared lower layer; `editor` depends on `editor-schema`; `editor-renderer` depends on the renderer-specific schema entry point and must not require the Vue editor package; `playground` consumes the published package interfaces for local validation; root workspace scripts build schema before editor and renderer, then the playground. Document the stable exports (`@i-prikot/editor-schema` and `/renderer`; `@i-prikot/editor` and its stylesheet exports; `@i-prikot/editor-renderer` plus its CSS assets) and distinguish them from internal source paths. **Expected behavior:** the document makes prohibited dependency inversions and public-versus-private boundaries explicit enough for future package changes. **Files:** `.ai-factory/ARCHITECTURE.md`. **Dependencies:** Task 1 establishes the shared package vocabulary. **Logging:** no runtime behavior changes and no new logs; document that `developmentDiagnostics` is opt-in and redacted, while normal integration diagnostics must not expose host tokens, document JSON/HTML, or message catalogs.

- [x] **Task 3: Define the Tinyfy host integration contract and reconcile both documents.** In `.ai-factory/ARCHITECTURE.md`, add a host-boundary section sourced from `NotionEditor`'s public API: the host owns `documentId`, `baseUrl`, current content and persistence, active anchor, theme class on the `.tinyfy-editor` root, optional image-upload adapter, locale/messages, and optional collaboration/AI configuration. Describe the supported component lifecycle: consume `ready`, `update`, `anchor-change`, and `operation-error`; use the exposed `getJSON`, `getHTML`, `focus`, and `setContent` methods only through the component ref; treat returned/emitted HTML as untrusted before host rendering. State that the host obtains short-lived credentials and configures external services, rather than placing secrets in package source or static client configuration. Include the separate static-render path through `@i-prikot/editor-renderer`. Then reconcile wording and cross-references in `.ai-factory/DESCRIPTION.md` so both artifacts use the same package names, host responsibility model, and Tinyfy terminology, without duplicating the full API reference. **Expected behavior:** Tinyfy integration ownership, security boundaries, and interactive/static consumption paths are unambiguous and do not rely on stale `src/editor` standalone-app paths. **Files:** `.ai-factory/ARCHITECTURE.md`, `.ai-factory/DESCRIPTION.md`. **Dependencies:** Task 2. **Logging:** no runtime behavior changes and no new logs; document the existing redaction expectation for diagnostics and operation errors without recording secrets or full document data.

### Phase 3: Documentation Validation
- [x] **Task 4: Validate the documentation against the current workspace contracts.** Review both changed documents against `package.json` workspace declarations, all four workspace manifests, public export maps, root build order, `packages/editor/src/components/notion/notion-editor/public-api.ts`, and `apps/playground/src/main.ts`. Confirm every listed package name, export path, dependency direction, and Tinyfy responsibility matches the source; remove stale standalone-application claims, deprecated `@tinyfy/*` package references, and internal paths presented as public APIs. Verify Mermaid diagrams render syntactically and internal markdown links resolve. Do not add or run test tasks because testing is disabled for this documentation-only update. **Expected behavior:** the two documents are internally consistent, traceable to present repository contracts, and safe for a Tinyfy integrator to follow. **Files:** `.ai-factory/DESCRIPTION.md`, `.ai-factory/ARCHITECTURE.md`. **Dependencies:** Tasks 1–3. **Logging:** no runtime behavior changes and no new logs; validation output, if produced locally, must remain limited to document paths and contract identifiers rather than content, credentials, or token values.
